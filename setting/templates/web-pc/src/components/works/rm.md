放置公用业务组件，比如：sellerFlags,areaSelector 等......

```
widgets
      |
      sellerFlags
          |
          index.temp
          style.less
          type.ts
```

index.temp

```tsx
export const SellerFlags=()=>(...);
```
