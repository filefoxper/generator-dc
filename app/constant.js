const ProjectType={
    WEB_PC:'web-pc'
}

const TestMode={
    DEPENDENT:'独立测试包',
    INDEPENDENT:'附属测试包'
}

const History={
    H5:'h5',
    HASH:'hash'
}

module.exports={
    ProjectType,
    TestMode,
    History
}